// Name of the plugin (must match the `name` of the package.json).
export const PLUGIN_NAME = 'homebridge-roborock-vacuum-update';

// The platform the plugin creates (see config.json).
export const PLATFORM_NAME = 'RoborockVacuumPlatformUpdate';
